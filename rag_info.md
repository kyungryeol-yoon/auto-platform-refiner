RAG 시스템을 구축할 때 "AI가 파일의 위치(경로)를 정확히 알려주는 것"은 사용자 편의성을 결정짓는 핵심 요소입니다. 단순히 README.md의 텍스트만 넣는 것이 아니라,
  '메타데이터(Metadata)'와 '경로 정보'를 전략적으로 주입해야 합니다.

  성공적인 RAG 구성을 위한 3가지 핵심 전략을 제안해 드립니다.

  ---

  1. 벡터 DB 등록 시 'Metadata' 활용 (필수)
  벡터 DB(ChromaDB, Pinecone, FAISS 등)에 데이터를 넣을 때, 텍스트(문서 내용) 외에 별도의 Metadata 필드를 반드시 사용해야 합니다.

   * 저장할 메타데이터 예시:
       * source_path: infra/api-gateway/kong-v2.0.1/README.md
       * category: api-gateway
       * project_name: kong
       * version: 2.0.1
   * 효과: AI가 답변할 때 "해당 정보는 {source_path}에서 찾았습니다"라고 정확한 위치를 찍어줄 수 있습니다.

  2. 텍스트 내용 안에 '경로'를 명시적으로 포함
  AI는 때로 메타데이터보다 본문 텍스트 내에 있는 정보를 더 잘 신뢰합니다. 따라서 생성되는 README.md의 최상단이나 최하단에 현재 경로를 텍스트로 박아 넣는 것이
  좋습니다.

   * README 예시:

   1     ---
   2     Location: /infra/api-gateway/kong-v2.0.1
   3     Category: api-gateway
   4     ---
   5     # Kong API Gateway 가이드
   6     ...
   * 효과: 검색된 조각(Chunk) 자체에 경로 정보가 포함되어 있어, AI가 별도의 추론 없이도 "이 폴더로 가세요"라고 말하게 됩니다.

  3. README 외에 어떤 파일을 더 넣어야 할까?
  README.md만 넣는 것이 가장 효율적이지만, 정보가 부족할 경우 다음 파일들도 함께 수집하는 것이 좋습니다.

   1. README.md (Main): 전체적인 맥락과 설치법 (가장 중요)
   2. troubleshooting.md (Optional): 과거 장애 대응 이력 (RAG에서 질문 답변율이 가장 높음)
   3. values.yaml 요약본: AI가 설정값(Port, IP 등)을 찾아야 할 경우 필요합니다. (전체를 넣기보다 중요한 라인만 요약해서 넣는 것을 추천)
   4. INDEX.md (Global Map): 전체 폴더 구조를 요약한 최상위 인덱스 파일. AI가 "사내에 어떤 도구들이 있어?"라는 광범위한 질문을 받았을 때 길잡이 역할을 합니다.

  ---

  💡 RAG 구축 시나리오 추천

  사용자님이 나중에 RAG를 구현하실 때 다음과 같은 흐름으로 진행해 보세요.

   1. 데이터 수집 (Ingestion):
       - 각 폴더를 순회하며 README.md를 읽습니다.
       - 이때 해당 파일의 실제 경로(Path)를 변수에 저장합니다.
   2. 청크 분할 (Chunking):
       - 큰 문서를 작은 단위로 쪼갭니다.
       - 중요: 쪼개진 모든 조각에 source_path 메타데이터를 동일하게 할당합니다.
   3. 프롬프트 구성 (System Prompt):
       - "너는 사내 인프라 전문가야. 답변할 때는 반드시 해당 정보가 들어있는 문서의 경로를 함께 알려줘."라고 지시합니다.

  결론: "폴더 위치도 알아야 할까요?" 에 대한 답변
  네, 반드시 알아야 합니다.
  하지만 AI에게 "알아서 찾아봐"라고 하기보다는, 데이터를 넣을 때 경로를 꼬리표(Metadata)로 붙여서 넣어주는 것이 정답입니다. 그래야만 사용자가 AI에게 "Kong
  설치법 어디 있어?"라고 물었을 때, "여기 방법입니다. 상세 파일은 infra/api-gateway/kong-v2.0.1 폴더에 있습니다"라고 답변할 수 있습니다.

  내일 회사에서 작성하시는 README.md에 경로 정보를 한 줄씩 포함하도록 프롬프트를 구성해 두었으니, 이 데이터들이 나중에 RAG의 훌륭한 밑거름이 될 것입니다!_